generator client {
  provider        = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Player {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  gameId Int
}
// when calling from the client we can send attack because we only need the gameid?
// the player service doesnt need to communicat with game
// in the game service do findGame {where: player1id or player2id}} matches param
// from send attack on client side